
<%= form_for @pokemonset, :html => { :class => 'form-horizontal' } do |f| %>
  

<% if @pokemonset.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@pokemonset.errors.count, "error") %> impidieron que se guardara el set:</h2>
 
    <ul>
    <% @pokemonset.errors.full_messages.each do |msg| %>
      <li><%=  msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

 <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field' %>
    </div>
  </div>
  <br>
<div class="control-group">
  <%= f.label :specie , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :specie, options_for_select(Pokemon.order('name').where(:local_language_id => 9).map{|s|[s.name, s.pokemon_species_id]}), include_blank: true , :class => 'text_field' %>
      </div>
</div>
 <div class="control-group">
  <%= f.label :ability , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :ability, options_for_select(Ability.order('name').where(:local_language_id => 9).map{|s|[s.name, s.ability_id]}), include_blank: true , :class => 'text_field' %>
      </div>
</div>

 
<div class="control-group">
  <%= f.label :item , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :item, options_for_select(Item.order('name').where(:local_language_id => 9).map{|s|[s.name, s.item_id]}), include_blank: true , :class => 'text_field' %>
      </div>
</div>
  
  <div class="control-group">
  <%= f.label :nature , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :nature, options_for_select(Nature.order('name').where(:local_language_id => 9).map{|s|[s.name, s.nature_id]}), include_blank: true , :class => 'text_field' %>
      </div>
</div>



 

            <div class="control-group">
    <%= f.label :hp, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :hp, :style => "width:30px;", :class => 'text_field' %>
    </div>
    </div>
  <div class="control-group">
    <%= f.label :atk, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :atk, :style => "width:30px;" ,:class => 'text_field' %>
    </div>
 </div>
  <div class="control-group">
    <%= f.label :defe, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :defe, :style => "width:30px;" ,:class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :spatk, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :spatk, :style => "width:30px;" ,:class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :spdef, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :spdef,:style => "width:30px;" , :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :spd, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :spd, :style => "width:30px;" ,:class => 'text_field' %>
    </div>
  </div>
     
        






 
 
  
  
  
 
  <div class="control-group">
  <%= f.label :move1 , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :move1, options_for_select(Move.order('name').where(:local_language_id => 9).map{|s|[s.name, s.move_id]}) , include_blank: true, :class => 'text_field' %>
      </div>
</div>
  <div class="control-group">
  <%= f.label :move2 , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :move2, options_for_select(Move.order('name').where(:local_language_id => 9).map{|s|[s.name, s.move_id]}), include_blank: true , :class => 'text_field' %>
      </div>
</div>
 <div class="control-group">
  <%= f.label :move3 , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :move3, options_for_select(Move.order('name').where(:local_language_id => 9).map{|s|[s.name, s.move_id]}) , include_blank: true, :class => 'text_field' %>
      </div>
</div>
  <div class="control-group">
  <%= f.label :move4 , :class => 'control-label' %>
      <div class="controls">
  <%= f.select :move4, options_for_select(Move.order('name').where(:local_language_id => 9).map{|s|[s.name, s.move_id]}) , include_blank: true, :class => 'text_field' %>
      </div>
</div>
  
    <%= f.label :comment, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :comment, :rows => 5, :style => "width:100%;" , :class => 'text_area' %>
    </div>
  

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pokemonsets_path, :class => 'btn' %>
  </div>

<% end %>

