<h1>Listing players</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Nick</th>
      <th>Pais</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
        <td><%= player.nickname %></td>
        <td><%= player.country %></td>
        <td><%= link_to 'Ver más', player %></td>        
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if current_user and current_user.admin %> 
<%= link_to 'New Player', new_player_path %>
<% end %>