Filtrar:
<%= form_tag '/players', :method => 'get' do %>
<%= country_select("player", "country", priority_countries: ["CL", "AR", "PE", "CO", "EC","MX","CR","BO","BR"]) %>
  <%= submit_tag t("search_desc.filter") , :name => nil, :class => "search-me btn btn-primary"  %>
<% end %>

<h1>Jugadores</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Nick</th>
      <th>Pais</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
        <td><%= player.nickname %></td>
        <td><%= player.country %></td>
        <td><%= link_to 'Ver más', player %></td>        
      </tr>
    <% end %>
  </tbody>
</table>
 <%= paginate @players , :pagination_class => "pagination-centered" %>
<br>
<% if current_user and current_user.admin %> 
<%= link_to 'New Player', new_player_path %>
<% end %>